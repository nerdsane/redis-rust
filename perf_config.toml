# Performance Configuration (generated by evolve harness)
# Temporarily set to 1 shard for Delancie Lua script compatibility
num_shards = 1

[response_pool]
capacity = 576
prewarm = 96

[buffers]
read_size = 8192
max_size = 1048576  # 1MB (fixed)

[batching]
min_pipeline_buffer = 70
batch_threshold = 6