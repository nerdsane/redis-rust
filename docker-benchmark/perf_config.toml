# Performance Configuration (generated by evolve harness)
num_shards = 16

[response_pool]
capacity = 576
prewarm = 96

[buffers]
read_size = 8192
max_size = 536870912  # 512MB (matches Redis proto-max-bulk-len)

[batching]
min_pipeline_buffer = 70
batch_threshold = 6